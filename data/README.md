# Data release location

Place the generated HDF5 split files here only if the total repository size remains suitable for GitHub. For large files, upload an immutable archive to Zenodo or OSF and place its DOI/URL in `../metadata/release_links.json`.

Do not alter the train, validation, or locked-test assignment after reporting the manuscript results.

# Configuration files

Copy `production_dataset_config_v1.json` and `closuregraph_cell9_repair_protocol_v2.json` into this directory. Preserve their checksums and exact values used in the manuscript.
